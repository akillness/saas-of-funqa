# 2026-05-05 Prompt Knowledge Loop Request

Immutable capture of the user request that established the per-prompt knowledge loop.

## Source

The user requested that every prompt be handled as a single combined step where:

- knowledge caused by the user's prompt is refined through Graphify;
- files are organized through Obsidian-compatible structure;
- the result is configured into llm-wiki;
- llm-wiki is then used as knowledge information for future work;
- the flow follows the `ooo` specification-first style.

## Handling Note

This source does not include secrets or external private files. It captures only the operational preference and workflow requirement.
