# 2026-05-05 Graphify Codex Install Request

Immutable capture of the user request to install Graphify for Codex.

## Source

The user requested:

```text
$graphify install codex
```

The request means Graphify should be available to Codex as an assistant-native integration, not merely as a one-shot local graph build.

