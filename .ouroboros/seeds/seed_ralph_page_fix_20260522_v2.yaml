goal: "Make the Ralph page work in the Next.js web app, document it in README, commit and push the change, then deploy Firebase App Hosting."
constraints:
  - "Do not touch secret files or Firebase service account keys."
  - "Keep the fix aligned with the existing App Router, i18n dictionary, and sidebar navigation patterns."
  - "Prefer a minimal route-level fix over broad visual or framework refactors."
  - "Use the existing App Hosting deployment command from package.json."
acceptance_criteria:
  - "`/ralph` renders without 404 in both default and localized `?lang=ko`/`?lang=en` flows."
  - "The sidebar navigation exposes the Ralph page."
  - "Ralph page copy is dictionary-backed in Korean and English."
  - "README documents the Ralph completion loop surface."
  - "`npm run typecheck` passes."
  - "`npm run build:web` passes."
  - "Local browser verification confirms the page renders."
  - "Changes are committed, pushed, and Firebase App Hosting deploy is attempted."
ontology_schema:
  name: RalphPageFixSpec
  description: Domain model for Ralph page fix
  fields:
    - name: goal
      type: string
      description: Goal of the task
      required: true
    - name: ralph_page_rendered
      type: boolean
      description: Whether the Ralph page renders successfully
      required: true
metadata:
  seed_id: seed_ralph_page_fix_20260522
  version: 1.0.0
  created_at: "2026-05-22T01:35:00Z"
  ambiguity_score: 0.12
  interview_id: null
  parent_seed_id: null
