rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // FunQA does not expose direct browser Storage access. Server-side Admin
    // SDK operations bypass rules and remain available when a storage-backed
    // scene implementation is introduced.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
